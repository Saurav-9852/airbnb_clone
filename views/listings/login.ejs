<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Travel Website | Login Page</title>
    <script src="https://cdn.tailwindcss.com"></script>

    <style>
        
        @keyframes fadeIn {
            from {
                opacity: 0;
                transform: translateY(-20px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .fade-in {
            animation: fadeIn 1s ease-in-out;
        }
    </style>
</head>
<body class="bg-red-400 text-black h-screen flex items-center justify-center">
    <div class="p-10 w-full max-w-md bg-gray-100 rounded-lg shadow-lg fade-in">
        <h2 class="text-red-600 text-4xl mb-10 font-bold text-center">Login to Traveling Website</h2>
        <form action="/listings/login" method="GET">
            <input class="block mb-5 w-full px-4 py-2 bg-transparent rounded-md border-2 border-gray-400 outline-none focus:border-blue-500 transition-colors duration-300"
                   type="email" placeholder="Email" name="email" required>
            <input class="block mb-5 w-full px-4 py-2 bg-transparent rounded-md border-2 border-gray-400 outline-none focus:border-blue-500 transition-colors duration-300"
                   type="password" placeholder="Password" name="password" required>
        </form>

        <!-- Added Login Button -->
        <div class="text-center mt-4">
            <a class="block mb-5 w-full px-4 py-2 bg-red-400 text-white rounded-md hover:bg-red-600 cursor-pointer font-semibold transition-colors duration-300 text-center"
               href="http://localhost:3000/listings">Login</a>
        </div>

        <div class="text-center mt-4">
            <a class="text-blue-400 hover:underline" href="http://localhost:3000/create">Create an Account</a>
        </div>
    </div>
    <script>
        tailwind.config = {
          theme: {
            extend: {},
          },
        }
      </script>
      
</body>
</html>
